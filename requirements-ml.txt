# requirements-ml.txt — dépendances ML (à ajouter à requirements.txt de base)
 
# ── Machine Learning ──────────────────────────────────────────────────────
mlflow==2.13.0
scikit-learn==1.5.0
xgboost==2.0.3
shap==0.45.0              # Interprétabilité XGBoost — SHAP values
optuna==3.6.1             # Hyperparameter tuning automatisé (optionnel)
imbalanced-learn==0.12.3  # SMOTE — rééquilibrage classes (fraude ~5%)
 
# ── OCR / Vision ──────────────────────────────────────────────────────────
pytesseract==0.3.10
opencv-python==4.9.0.80
Pillow==10.3.0
 
# ── Data ──────────────────────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
pyarrow==15.0.2           # Lecture tables Delta/Parquet depuis ADLS (Ajusté pour MLflow)
 
# ── Azure ─────────────────────────────────────────────────────────────────
azure-storage-file-datalake==12.14.0