Instructions d'Exécution : Plateforme Data KiVendTout (End-to-End)Tu es un expert Data Engineer/MLOps. Ton objectif est d'exécuter, déboguer et valider l'architecture complète de la plateforme KiVendTout.1. Contexte de l'ArchitectureLe projet suit un pattern Medallion Lakehouse:  Bronze : Données brutes de Kafka et images CNI (Azure ADLS).  Silver : Nettoyage Spark, anonymisation RGPD (Presidio) et mise à jour KYC.  Gold : Schéma en étoile (dbt), Feature Store ML et Scores de fraude.  2. Ordre de Lancement Impératif (Workflow)Respecte strictement cet ordre pour éviter les erreurs de dépendances :Phase A : Infrastructure & StreamingActiver l'environnement : source .venv/bin/activate.Lancer les services : Kafka (Topics : payments-raw, clickstream-raw) et Airflow.Démarrer MLflow (Port 5000) : Nécessaire pour le registre de modèles.  Bashmlflow server --backend-store-uri sqlite:///mlflow.db --default-artifact-root ./mlruns --host 0.0.0.0 --port 5000
Simuler les flux : Lancer les producteurs dans ingestion/.  Ingestion Bronze : Lancer spark/bronze/kafka_to_bronze.py.  Phase B : Intelligence Artificielle (Étape 5)Entraînement : Exécuter ml/fraud_detection/train.py.  Contrainte : Doit lire depuis gold/fraud_feature_store.  Validation : F1-score > 0.90 pour promotion en "Production".  Inférence : Lancer ml/fraud_detection/predict_streaming.py.  Fonctionnement : Chargement du modèle "Champion" via MLflow et scoring des messages Kafka.  Phase C : Pipeline OCR & KYCOCR Dépendance : Vérifier que tesseract-ocr est installé sur le système.  DAG Airflow : Déclencher ml_ocr_cni_processing pour traiter les images bronze/images_cni.  3. Débogage et Points de VigilanceVariables d'Environnement : Vérifier la présence de AZURE_STORAGE_ACCOUNT_KEY et MLFLOW_TRACKING_URI dans le .env.  Schémas Spark : L'ordre des colonnes dans FEATURE_COLS doit être identique entre train.py et predict_streaming.py.  RGPD : S'assurer que les images sont supprimées de la couche Bronze après le passage de l'OCR.  dbt : Avant de lancer la Gold, exécuter dbt compile dans le dossier dbt/ pour valider le graphe.  4. Résultats Attendus pour ValidationMLflow : Modèle fraud-xgboost-champion versionné et en stage "Production".  ADLS Gold : Présence des scores dans gold/fraud_scores/ (Format Delta).  Airflow : Tous les DAGs (silver, gold, ml_retrain, ocr) doivent être au vert.